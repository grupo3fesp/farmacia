{
  "name": "assistente-farmacia-municipal",
  "version": "0.1.0",
  "private": true,
  "description": "Assistente virtual informativo de disponibilidade de medicamentos da Farmácia Municipal (protótipo).",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "seed": "node scripts/gerar-seed.mjs",
    "build": "node build.mjs",
    "start": "node src/index.ts",
    "dev": "node --watch src/index.ts",
    "test": "node --test \"tests/**/*.test.ts\""
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.32.1",
    "@supabase/supabase-js": "^2.45.4"
  },
  "devDependencies": {
    "@types/node": "^22.7.4",
    "esbuild": "^0.28.2",
    "typescript": "^5.6.2"
  }
}
